
%--------------------------------------
% Onde incluir todos os ficheiros (package)
% e instruções adicionais para este projeto
%
%-------------------
% Obs#1 - referências bibliográficas
% Descomentar se quiser usar o estilo APA para as referências bibliográficas
%\usepackage{apacite}

%-------------------
% Hipertexto, links e endereços web
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\hypersetup{breaklinks=true}

%-------------------
% Legendas (tabelas e figuras)
\usepackage{caption}
\usepackage{subcaption}
%---------------------

% Deve colocar neste espaço todas as packages e comandos adicionais que considerar necessários acrescentar

% Tabelas (traços)
\usepackage{tabularray}

% Traços em tabelas
\makeatletter
\def\hlinewd#1{%
\noalign{\ifnum0=`}\fi\hrule \@height #1 \futurelet
\reserved@a\@xhline}
\makeatother

% Definição, exemplo, teorema e proposição
\newtheorem{definition}{Defini\c{c}\~{a}o}
\newtheorem{example}{Exemplo}
\newtheorem{theorem}[definition]{Teorema}
\newtheorem{proposition}{Proposi\c{c}\~{a}o}

%--------------------------------------

