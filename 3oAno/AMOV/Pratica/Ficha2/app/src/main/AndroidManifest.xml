<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ficha2"
        tools:targetApi="31">

        <activity android:name="MyActivity"
            android:exported="true">

            <intent-filter> <!-- com o intent-filter dizemos oq ue é que a atividade pode fazer -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- temos que dizer isto(exported) para ele poder ser usado de fora-->
    </application>
</manifest>

<!-- no android:roundIcon -> existem varias pastas com aquele icon que tem o mesmo nome e um resource manager é que vai saber qual carregar consoante o dispositivo onde tem que ser carregado -->
<!-- supportsRtl é support right left para o caso dos paises que escreve da direita para a esquerda -->